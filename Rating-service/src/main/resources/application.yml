
server:
  port: 8083

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: ratingDb
  application:
    name: rating-Server
  config:
    import:
      - configserver:http://localhost:8086

